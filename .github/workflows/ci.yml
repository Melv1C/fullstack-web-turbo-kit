name: Main CI/CD Pipeline

on:
  push:
    branches:
      - main
      - chore/reusable-CI
    tags:
      - "*@*"
jobs:
  ci:
    uses: melv1c/github-workflows/.github/workflows/ci.yml@main
    with:
      package-manager: bun
