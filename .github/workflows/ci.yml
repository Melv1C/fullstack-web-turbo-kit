name: CI

on:
  pull_request:
    branches: [main]
    paths:
      - 'apps/**'
      - 'packages/**'
      - '.github/workflows/ci.yml'
  workflow_call:

jobs:
  ci:
    name: Build & Check
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/postgres

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate committed web env files
        run: |
          set -euo pipefail

          files=(
            "apps/frontend/.env.local"
            "apps/frontend/.env.staging"
            "apps/frontend/.env.production"
            "apps/admin/.env.local"
            "apps/admin/.env.staging"
            "apps/admin/.env.production"
          )

          for file in "${files[@]}"; do
            if [ ! -f "$file" ]; then
              echo "Missing required env file: $file"
              exit 1
            fi

            if awk -F '=' '
              /^[[:space:]]*($|#)/ { next }
              $1 !~ /^VITE_[A-Z0-9_]+$/ { print; exit 1 }
            ' "$file"; then
              true
            else
              echo "Invalid key found in $file. Only VITE_* keys are allowed."
              exit 1
            fi
          done

      - name: Format check
        run: pnpm run format:check

      - name: Prisma Validate
        run: pnpm run prisma:validate

      - name: Prisma Generate
        run: pnpm run prisma:generate

      - name: Build all packages
        run: pnpm run build

      - name: Linting
        run: pnpm run lint
