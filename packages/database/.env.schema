# @import(../../.env.shared)
# @defaultSensitive=true @defaultRequired=true
# ---

# @type=url
DATABASE_URL=if(
  forEnv(development, test), postgresql://postgres:postgres@localhost:5432/postgres, 
  undefined
)
